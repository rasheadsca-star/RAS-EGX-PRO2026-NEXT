# Architecture Contract
EGX ONE is a clean-room, single authoritative pipeline: DATA → READINESS → FEATURES → REGIME → CANDIDATES → QUALITY → ENTRY → RISK → RANKING → FINAL DECISION → IMMUTABLE SNAPSHOT. No frontend scoring or legacy runtime dependency is allowed. Critical failures fail closed.
