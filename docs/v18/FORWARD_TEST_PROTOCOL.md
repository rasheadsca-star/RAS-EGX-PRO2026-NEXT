# Forward test protocol

V18 enters only as `V18_SHADOW`. Each pre-outcome snapshot is immutable and hash-addressed. Entry, target, stop, horizon, costs and ambiguity policy are frozen before the next session. V16/V17 ledgers remain untouched. Promotion is manual and requires explicit approval.
