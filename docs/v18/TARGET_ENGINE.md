# Target engine

The baseline target is the greater of one ATR above entry and prior 20-session structural resistance plus a small volatility buffer. This is deliberately not a fixed ATR target alone. Future versions may replace it only through a separately versioned, pre-registered MFE-quantile experiment.
