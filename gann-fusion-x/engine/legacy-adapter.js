(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.GFXLegacy=api;})(typeof self!=='undefined'?self:this,function(){'use strict';function fromEgxPro(snapshot){const s=snapshot||{},out={source:'EGX_PRO',sessionDate:s.sessionDate||s.freshness?.decisionSession||null,fresh:Boolean(s.freshness?.isFresh||s.currentSessionReady),marketScan:s.marketScan||{},recommendations:{},momentumWatch:{},meta:{status:s.status,statusAr:s.statusAr,priceTruth:s.priceTruth||{},guardrails:s.guardrails||{}}};for(const r of s.recommendations||[]){out.recommendations[r.ticker]={ticker:r.ticker,nameAr:r.companyNameAr||r.ticker,close:Number(r.close||0),rsi14:Number(r.rsi14||0),volumeRatio20:Number(r.volumeRatio20||0),riskReward:Number(r.riskReward||0),entryLow:Number(r.entryLow||0),entryHigh:Number(r.entryHigh||0),stopLoss:Number(r.stopLoss||0),target1:Number(r.target1||0),referenceStatus:r.status||null}}for(const r of s.extendedMomentumWatch||[]){out.momentumWatch[r.ticker]={ticker:r.ticker,nameAr:r.companyNameAr||r.ticker,close:Number(r.close||0),rsi14:Number(r.rsi14||0),volumeRatio20:Number(r.volumeRatio20||0),ret5Pct:Number(r.ret5Pct||0),ret20Pct:Number(r.ret20Pct||0),riskReward:Number(r.riskReward||0)}}return out}function mergeEvidence(base,e){const x={...(base||{})};if(e){x.nameAr=x.nameAr||e.nameAr;x.legacyEvidence={...(x.legacyEvidence||{}),egxPro:e};if(!x.fundamentals&&Number.isFinite(e.qualityScore))x.fundamentals={score:e.qualityScore}}return x}async function fetchJson(url){const res=await fetch(url,{cache:'no-store'});if(!res.ok)throw new Error(`HTTP ${res.status}`);return res.json()}return{fromEgxPro,mergeEvidence,fetchJson};});