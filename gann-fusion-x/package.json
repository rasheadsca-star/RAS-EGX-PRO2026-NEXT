{
  "name": "egx-gann-fusion-x",
  "version": "1.0.0",
  "private": true,
  "description": "Independent Gann timing and explainable EGX research engine",
  "scripts": {
    "test": "node scripts/self-test.cjs",
    "sync:sepa": "node scripts/sync-sepa.cjs",
    "build:snapshot": "node scripts/build-snapshot.cjs",
    "refresh": "npm run sync:sepa && npm run build:snapshot",
    "critic": "node review/critic.cjs",
    "audit:isolation": "node scripts/isolation-audit.cjs",
    "verify": "npm run test && npm run critic && npm run audit:isolation"
  }
}