#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path'),root=path.resolve(process.env.GITHUB_WORKSPACE||'.'),file=path.join(root,'data/v17/historical-recovery/long-history/compact-market.json'),issues=[];if(!fs.existsSync(file))issues.push('MISSING');else{const size=fs.statSync(file).size,x=JSON.parse(fs.readFileSync(file,'utf8'));if(size>=10*1024*1024)issues.push('TOO_LARGE');if(x.results?.some(r=>r.sessions||r.rawOHLCV))issues.push('RAW_HISTORY_LEAK');if(x.results?.length!==x.summary?.canonicalOrdinaryEquities)issues.push('UNIVERSE_MISMATCH');if(!x.researchOnly)issues.push('NOT_RESEARCH_ONLY');for(const r of x.results||[])if(r.dataQualityStatus==='VALID'&&!r.horizons?.maxAvailable)issues.push(`${r.ticker}:MISSING_HORIZONS`);console.log(JSON.stringify({valid:issues.length===0,sizeBytes:size,resultCount:x.results?.length||0,issues},null,2))}if(issues.length)process.exit(1);
