#!/usr/bin/env node
'use strict';
const fs=require('fs'),path=require('path');
const ROOT=process.cwd(),NOW=new Date().toISOString(),p=(...x)=>path.join(ROOT,...x);
const read=(f,d)=>{try{return JSON.parse(fs.readFileSync(f,'utf8'))}catch{return d}},write=(f,o)=>{fs.mkdirSync(path.dirname(f),{recursive:true});fs.writeFileSync(f,JSON.stringify(o,null,2)+'\n')};
const rowsOf=x=>Array.isArray(x)?x:Array.isArray(x?.rows)?x.rows:Array.isArray(x?.items)?x.items:Array.isArray(x?.data)?x.data:[];
const num=v=>{if(v==null||v==='')return null;const n=Number(String(v).replace(/[,%٬،]/g,'').replace(/[^0-9.+\-eE]/g,''));return Number.isFinite(n)?n:null};
const first=(...v)=>v.find(x=>x!==null&&x!==undefined&&x!=='')??null,sym=v=>String(v||'').toUpperCase().replace(/\.CA$/,'').replace(/[^A-Z0-9.]/g,''),clean=s=>String(s||'').replace(/\s+/g,' ').split(/End 1\s*-->/i).pop().replace(/-->/g,'').replace(/^[\d,\[\]\s:'"#]+/,'').trim();
const gradeRank=g=>({P1:5,P2:4,P3:3,Watch:2,Blocked:1}[g]||0);
function latestHistorySession(h){const d=[];for(const rows of Object.values(h?.symbols||{})){if(!Array.isArray(rows))continue;for(const r of rows){const x=String(r?.date||'');if(/^\d{4}-\d{2}-\d{2}$/.test(x))d.push(x)}}return d.sort().at(-1)||null}
function externalMatch(sr){const states=[sr?.externalValidation?.direct?.state,sr?.externalValidation?.rendered?.state];return states.includes('CURRENT_MATCH')}
function main(){
  const marketObj=read(p('data/market.json'),{}),market=rowsOf(marketObj),history50=read(p('data/history-50.json'),{}),sessionDate=latestHistorySession(history50),ranking=rowsOf(read(p('data/final-opportunity-ranking.json'),{rows:[]})),old=read(p('data/today-decision-center.json'),{}),internal=read(p('data/v17/internal-ohlc-support-resistance.json'),{});
  const marketMap=new Map(market.map(r=>[sym(r.symbol),r])),internalMap=new Map(rowsOf(internal).map(r=>[sym(r.symbol),r]));
  const opportunities=ranking.filter(r=>sym(r.symbol)&&num(r.price)>0).map((r,i)=>{
    const symbol=sym(r.symbol),m=marketMap.get(symbol)||{},sr=internalMap.get(symbol)||null,srAvailable=Boolean(sr),internalExecution=Boolean(internal.executionCandidateReady&&sr?.executionEligible===true),marketExecution=m.executionAllowed!==false,blocked=r.grade==='Blocked'||r.precisionRisk===true||num(r.price)<=0,executionAllowed=Boolean(!blocked&&internalExecution&&marketExecution),opportunityState=executionAllowed?'EXECUTABLE':blocked?'BLOCKED':'CONDITIONAL_WATCH',confidence=Math.round(num(first(r.targetProbability,r.finalScore,r.finalConfidence,r.confidence))||0);
    return {rank:i+1,symbol,name:clean(first(r.name,m.name,m.name_ar,m.name_en,r.symbol)),grade:r.grade||'Watch',opportunityState,label:executionAllowed?'تنفيذ مشروط':blocked?'مستبعد':'مراقبة مشروطة',price:num(first(r.price,m.price,m.lastPrice)),entryFrom:num(first(r.entryFrom,r.entryLow,r.entry)),entryTo:num(first(r.entryTo,r.entryHigh,r.entry)),target1:num(r.target1),target2:num(r.target2),stopLoss:num(r.stopLoss),support1:num(sr?.support1??sr?.levels?.support1?.value??r.support1),support2:num(sr?.support2??sr?.levels?.support2?.value),resistance1:num(sr?.resistance1??sr?.levels?.resistance1?.value??r.resistance1),resistance2:num(sr?.resistance2??sr?.levels?.resistance2?.value),pivot:num(sr?.pivot),supportResistance:sr,srVerified:externalMatch(sr),provisionalPlan:!executionAllowed,confidence,finalScore:num(r.finalScore),targetProbability:num(r.targetProbability),rr:num(first(r.rr,r.riskReward)),potentialProfitPct:num(r.potentialProfitPct),executionAllowed,monitorOnly:!executionAllowed,priceState:r.priceState||null,historySessions:num(r.historySessions)||0,why:r.why||r.executionBlockReason||m.exclusionReason||'فرصة مرتبة تحتاج تحققًا قبل التنفيذ.',reason:executionAllowed?'اجتازت بوابات التنفيذ الحالية باستخدام S/R داخلي موثق مع بقاء Mubasher مصدر تحقق/مقارنة فقط.':srAvailable?`دعم/مقاومة داخلي موثق من جلسة مكتملة ${sr.sessionDate||'غير معلومة'}؛ المتابعة بحثية حتى تكتمل بوابات التنفيذ.`:'لا توجد مستويات دعم ومقاومة داخلية قابلة للتدقيق؛ تُعرض للمراقبة فقط.'};
  }).sort((a,b)=>(b.opportunityState==='EXECUTABLE')-(a.opportunityState==='EXECUTABLE')||gradeRank(b.grade)-gradeRank(a.grade)||b.confidence-a.confidence||(b.potentialProfitPct||0)-(a.potentialProfitPct||0)).map((r,i)=>({...r,rank:i+1})).slice(0,80);
  const executable=opportunities.filter(r=>r.opportunityState==='EXECUTABLE'),watch=opportunities.filter(r=>r.opportunityState==='CONDITIONAL_WATCH'),blocked=opportunities.filter(r=>r.opportunityState==='BLOCKED'),srRows=opportunities.filter(r=>r.supportResistance),externalMatches=srRows.filter(r=>r.srVerified),executionSr=srRows.filter(r=>r.supportResistance?.executionEligible===true),srPct=opportunities.length?Number((srRows.length/opportunities.length*100).toFixed(2)):0,externalPct=opportunities.length?Number((externalMatches.length/opportunities.length*100).toFixed(2)):0;
  const mainDecision=executable.length?`توجد ${executable.length} فرصة تنفيذية مشروطة و${watch.length} فرصة متابعة`:opportunities.length?`توجد ${opportunities.length} فرصة مرتبة للمتابعة، ولا توجد توصية تنفيذية آمنة الآن`:'لم يتم توليد ترتيب فرص صالح';
  const decision={ok:true,engine:'goal_reconciled_ranked_opportunities_v17_internal_sr',generatedAt:NOW,sessionDate,sessionTruth:{sessionDate,historySource:'data/history-50.json',historyGeneratedAt:history50.generatedAt||null,marketGeneratedAt:marketObj.generatedAt||marketObj.updatedAt||null,explicitSessionAttached:Boolean(sessionDate),completedOhlcSession:internal.levelSessionDate||null,currentSessionCompletionConfirmed:internal.sessionCompletionConfirmed===true},mainDecision,caution:'فرص المتابعة ليست أوامر شراء. S/R الأساسي مشتق داخليًا من OHLC لجلسة مكتملة مع provenance/freshness/confidence؛ Mubasher مصدر تحقق ومقارنة وليس اعتمادًا صلبًا. التنفيذ لا يُسمح به إلا إذا اجتازت بوابات الجلسة والجودة والسيولة والتغطية والتعارضات.',summary:{rankedCount:opportunities.length,executionCount:executable.length,conditionalWatchCount:watch.length,blockedCount:blocked.length,marketRows:market.length,supportResistanceCoverageCount:srRows.length,supportResistanceCoveragePct:srPct,supportResistanceExternalMatchCount:externalMatches.length,supportResistanceExternalMatchPct:externalPct,supportResistanceExecutionCandidateCount:executionSr.length,internalCoveragePct:num(internal.coveragePct),internalFreshnessPct:num(internal.freshnessPct),internalCriticalFieldsPct:num(internal.criticalFieldsPct)},rankedOpportunities:opportunities,executableOpportunities:executable.slice(0,15),conditionalWatch:watch.slice(0,30),blockedPreview:blocked.slice(0,20),supportResistancePolicy:{primarySource:'INTERNAL_OHLC_PIVOT',mubasherRole:'VALIDATION_COMPARISON_NOT_HARD_DEPENDENCY',currentCompletedSessionRequiredForExecution:true,qualityGateRequiredForExecution:true,criticalSourceConflictBlocksExecution:true,automaticPromotion:false},legacyDecision:{engine:old.engine||null,generatedAt:old.generatedAt||null,sessionDate:old.sessionDate||null,mainDecision:old.mainDecision||null}};
  write(p('data/today-decision-center.json'),decision);console.log(sessionDate,mainDecision,decision.summary);
}
main();
