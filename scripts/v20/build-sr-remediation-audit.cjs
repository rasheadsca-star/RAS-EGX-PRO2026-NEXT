#!/usr/bin/env node
'use strict';
const fs=require('fs'),path=require('path');const root=path.resolve(process.env.GITHUB_WORKSPACE||'.'),P=r=>path.join(root,r),read=(r,f=null)=>{try{return JSON.parse(fs.readFileSync(P(r),'utf8'))}catch{return f}},write=(r,v)=>fs.writeFileSync(P(r),`${JSON.stringify(v,null,2)}\n`),finite=v=>{if(v===null||v===undefined||v==='')return null;const n=Number(v);return Number.isFinite(n)?n:null};
function valid(r){const o=finite(r.open),h=finite(r.high),l=finite(r.low),c=finite(r.close);return o>0&&h>0&&l>0&&c>0&&h>=Math.max(o,c,l)&&l<=Math.min(o,c,h)}
function history(symbol,session){const d=read(`data/history/${symbol}.json`,null);if(!d)return{exists:false,state:'HISTORY_FILE_MISSING',symbolVerified:false,rowCount:0,validOhlcCount:0,latestSession:null,currentSession:false,source:null};const rows=Array.isArray(d.sessions)?d.sessions:[],latest=rows.map(x=>x.date).filter(Boolean).sort().at(-1)||null;return{exists:true,state:d.symbolVerified===true?'HISTORY_IDENTITY_VERIFIED':'HISTORY_IDENTITY_UNVERIFIED',symbolVerified:d.symbolVerified===true,rowCount:rows.length,validOhlcCount:rows.filter(valid).length,latestSession:latest,currentSession:latest===session,source:d.primarySource||d.symbolVerification?.provider||null,verification:d.symbolVerification||null}}
function diagnose(h,sr,session){if(!h.exists)return'HISTORY_FILE_MISSING';if(!h.symbolVerified)return'HISTORY_IDENTITY_UNVERIFIED';if(h.rowCount<30)return'HISTORY_ROWS_INSUFFICIENT_FOR_SR';if(!h.currentSession)return'HISTORY_NOT_REFRESHED_TO_CURRENT_SESSION';if(!sr)return'INTERNAL_SR_ROW_MISSING_DESPITE_CURRENT_HISTORY';if(sr.trustedProvenance!==true)return'INTERNAL_SR_PROVENANCE_NOT_TRUSTED';if(sr.levelSessionDate!==session)return'INTERNAL_SR_LEVEL_SESSION_STALE';if((sr.blockers||[]).length)return'INTERNAL_SR_ROW_BLOCKED';return'AGGREGATE_CANDIDATE_MISSING_REQUIRES_V17_REBUILD_REVIEW'}
const gate=read('data/v17/resilient-session-status.json',{}),sr=read('data/v17/internal-ohlc-support-resistance.json',{}),market=read('data/v20/market-explorer.json',{rows:[]}),session=gate.priceTruth?.verifiedSessionDate||sr.referenceSessionDate,missing=Array.isArray(sr.missingCandidateSymbols)?sr.missingCandidateSymbols:(gate.missingSymbols||[]),srMap=new Map((sr.rows||[]).map(x=>[x.ticker,x])),mMap=new Map((market.rows||[]).map(x=>[x.ticker,x]));const symbols=missing.map(symbol=>{const h=history(symbol,session),r=srMap.get(symbol)||null,m=mMap.get(symbol)||null;return{symbol,diagnosis:diagnose(h,r,session),currentMarket:{sessionDate:m?.sessionDate||null,currentSessionAvailable:m?.currentSessionAvailable===true,price:finite(m?.price),dataQualityState:m?.dataQualityState||null,sourceConflict:m?.sourceConflict===true},history:h,internalSr:r?{rowExists:true,levelSessionDate:r.levelSessionDate||null,sourceSessionDate:r.sourceSessionDate||null,source:r.source||null,methodology:r.methodology||null,confidence:finite(r.confidence),trustedProvenance:r.trustedProvenance===true,externalValidationState:r.externalValidationState||null,blockers:Array.isArray(r.blockers)?r.blockers:[]}: {rowExists:false},automaticMutationPerformed:false}}
const t=sr.thresholds||{},total=Number(sr.candidateUniverseCount||0),req=(p)=>Math.ceil(total*Number(p)/100-1e-9),criticalEquivalent=Math.round(total*Number(sr.criticalFieldsPct||0)/100),conflicts=Array.isArray(sr.sourceConflicts)?sr.sourceConflicts:[];const out={schemaVersion:'20.0.0-sr-remediation-audit-1',generatedAt:new Date().toISOString(),sessionDate:session,readOnly:true,automaticV17MutationAllowed:false,guaranteesExecutionGrade:false,requiresV17InternalSrRebuild:true,requiresV17GateRebuild:true,currentGap:{candidateUniverseCount:total,trustedCount:Number(sr.candidateTrustedCount||0),trustedFreshCount:Number(sr.candidateTrustedFreshCount||0),criticalEquivalent,requiredTrustedCount:req(t.minimumCandidateCoveragePct||95),requiredTrustedFreshCount:req(t.minimumCandidateFreshnessPct||98),requiredCriticalEquivalent:req(t.minimumCandidateCriticalFieldsPct||95),trustedGap:Math.max(0,req(t.minimumCandidateCoveragePct||95)-Number(sr.candidateTrustedCount||0)),trustedFreshGap:Math.max(0,req(t.minimumCandidateFreshnessPct||98)-Number(sr.candidateTrustedFreshCount||0)),criticalGap:Math.max(0,req(t.minimumCandidateCriticalFieldsPct||95)-criticalEquivalent),averageFreshConfidence:finite(sr.averageFreshConfidence),minimumAverageFreshConfidence:finite(t.minimumAverageFreshConfidence),sourceConflictCount:conflicts.length},missingSymbols:missing,symbols,sourceConflicts:conflicts,interpretation:{necessaryRemediationOnly:true,sufficientForExecutionGrade:false,note:'The audit diagnoses current evidence gaps only. Any remediation must be followed by the authoritative V17 Internal S/R rebuild and resilient-session gate rebuild; V20 does not write V17 evidence.'}};write('data/v20/sr-remediation-audit.json',out);console.log(JSON.stringify({sessionDate:session,missingSymbols:missing,diagnoses:Object.fromEntries(symbols.map(x=>[x.symbol,x.diagnosis])),gap:out.currentGap},null,2));