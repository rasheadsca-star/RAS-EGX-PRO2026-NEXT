import { buildUniverseRegistry } from './universe.js';
import { evaluatePhase3Gate } from './phase3-gate.js';
import { validateExchangeCalendar } from './session-authority.js';
import { observationBarHash,verifyMarketObservationCertificate } from './market-observation-certification.js';
import { fullUniverseReportHash } from './phase-transition.js';
export function runFullUniverseReadiness({universe,sessionAuthority,exchangeCalendar,barsByTicker={},flagsByTicker={},acquisitionPlans=[],sourceReceipts=[],minHistory=100,minMedianTurnover=0}={}){const structural=[];if(!universe||universe.state!=='READY')structural.push(`UNIVERSE:${universe?.state??'MISSING'}`);if(!sessionAuthority||sessionAuthority.state!=='READY'||!sessionAuthority.currentSession)structural.push(`SESSION_AUTHORITY:${sessionAuthority?.state??'MISSING'}`);try{validateExchangeCalendar(exchangeCalendar)}catch(e){structural.push(`EXCHANGE_CALENDAR:${e.message}`)}if(exchangeCalendar?.version&&sessionAuthority?.calendarVersion!==exchangeCalendar.version)structural.push(`CALENDAR_VERSION_MISMATCH:${sessionAuthority?.calendarVersion??'MISSING'}:${exchangeCalendar.version}`);if(exchangeCalendar?.sessions&&!exchangeCalendar.sessions.some(x=>x.session===sessionAuthority?.currentSession))structural.push(`CURRENT_SESSION_NOT_IN_CALENDAR:${sessionAuthority?.currentSession??'MISSING'}`);const universeTickers=new Set((universe?.rows??[]).map(x=>x.ticker)),dataTickers=Object.keys(barsByTicker??{}),extras=dataTickers.filter(t=>!universeTickers.has(t)).sort();if(extras.length)structural.push(`DATA_OUTSIDE_UNIVERSE:${extras.join(',')}`);const planByTicker=new Map(acquisitionPlans.map(x=>[x.ticker,x])),receiptByKey=new Map(sourceReceipts.map(x=>[`${x.ticker}|${x.sourceId}`,x]));if(!structural.length){for(const ticker of universeTickers){const bars=barsByTicker[ticker]??[],plan=planByTicker.get(ticker);if(!bars.length||!plan?.primary)continue;const latest=bars[bars.length-1],expectedBarHash=observationBarHash({ticker,session:latest.session,open:latest.open,high:latest.high,low:latest.low,close:latest.close,volume:latest.volume}),primary=receiptByKey.get(`${ticker}|${plan.primary}`);if(!primary){structural.push(`PRIMARY_OBSERVATION_RECEIPT_MISSING:${ticker}`);continue}const cert=verifyMarketObservationCertificate(primary,{ticker,session:sessionAuthority.currentSession,sourceId:plan.primary,expectedBarHash,acquisitionPlan:plan});if(cert.state!=='READY')structural.push(`PRIMARY_BAR_CERTIFICATE:${ticker}:${cert.reasons.join('|')}`)}}if(structural.length){const phase3=evaluatePhase3Gate({universe,registry:null,sessionAuthority,acquisitionPlans,sourceReceipts});return freeze({state:'FAIL',structuralBlockers:structural.sort(),registry:null,phase3,coverage:null,reportHash:null})}const allowedSessions=new Set(exchangeCalendar.sessions.filter(x=>x.session<=sessionAuthority.currentSession).map(x=>x.session));const symbols=universe.rows.map(member=>{const flags=flagsByTicker[member.ticker]??{};return{ticker:member.ticker,companyName:member.companyName??null,bars:barsByTicker[member.ticker]??[],sourceStatus:flags.sourceStatus??'UNKNOWN',conflict:flags.conflict===true,suspended:flags.suspended===true,corporateActionReview:flags.corporateActionReview===true}});const registry=buildUniverseRegistry(symbols,{latestSession:sessionAuthority.currentSession,minHistory,minMedianTurnover,allowedSessions});const phase3=evaluatePhase3Gate({universe,registry,sessionAuthority,acquisitionPlans,sourceReceipts});const covered=registry.rows.filter(r=>r.readiness!=='SOURCE_UNAVAILABLE').length,coverage={universeTotal:universe.total,registryTotal:registry.total,withAnyBars:covered,missingBars:registry.total-covered,ready:registry.counts.READY??0,readinessCounts:registry.counts};const observationCertificateHashes=sourceReceipts.map(r=>r.observationCertificateHash).filter(Boolean).sort();const report={state:phase3.verdict==='PASS'?'PASS':'FAIL',session:sessionAuthority.currentSession,calendarVersion:exchangeCalendar.version,universeVersion:universe.version??null,registryVersion:registry.version,structuralBlockers:[],registry,phase3,coverage,observationCertificateHashes};report.reportHash=fullUniverseReportHash(report);return freeze(report)}function freeze(v){return Object.freeze(v)}
