import { sha256 } from './hash.js';
const REQUIRED=['marketSession','exchangeCalendarVersion','universeVersion','rawDataVersion','normalizedDataVersion','sourceManifest','corporateActionVersion','fundamentalsCutoff','featureVersion','modelVersion','engineVersion','configVersion','commitHash','generatedAt'];
export function createSessionManifest(input){for(const key of REQUIRED){if(input[key]===undefined||input[key]===null||input[key]==='')throw new Error(`manifest_missing:${key}`)}const manifest=structuredClone(input);manifest.snapshotHash=sha256(manifest);return Object.freeze(manifest)}
export function validateSessionManifest(m){if(!m||typeof m!=='object')return{valid:false,errors:['manifest_not_object']};const errors=REQUIRED.filter(k=>m[k]===undefined||m[k]===null||m[k]==='').map(k=>`manifest_missing:${k}`);if(!m.snapshotHash)errors.push('manifest_missing:snapshotHash');else{const{snapshotHash,...rest}=m;if(sha256(rest)!==snapshotHash)errors.push('snapshot_hash_mismatch')}return{valid:errors.length===0,errors}}
