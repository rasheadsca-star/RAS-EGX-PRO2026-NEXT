'use strict';
const fs=require('fs');
const P=require('../../astra/pipeline/g09-unified-decision-pipeline.cjs');
const {listStrategyIds,getStrategyDescriptor}=require('../../astra/strategies/strategy-registry.cjs');
const SPECS=Object.freeze(Object.fromEntries(listStrategyIds().map(id=>[id,getStrategyDescriptor(id)])));
function trainSessions(count=50,size=60){return Array.from({length:count},(_,d)=>Array.from({length:size},(_,i)=>({sessionDate:`2026-${d<28?'07':'08'}-${String(1+(d%28)).padStart(2,'0')}`,ticker:`T${String(i).padStart(2,'0')}`,xNew:[1,(i-size/2)/size,(d%5)/5],yTop10:i<10?1:0,yNetPositive:(i+d)%3!==0?1:0,yLargeLoss:(i+d)%19===0?1:0,validationStatus:'VALID',migrationValidationStatus:'VALID'})))}
function currentRows(size=80,low=false){return Array.from({length:size},(_,i)=>({sessionDate:'2026-09-15',ticker:`T${String(i).padStart(2,'0')}`,xNew:[1,(i-30)/60,.4],momentumFailureRisk:.1,effectiveSupport:1.2,turnover20:low?500000:30000000,rsi14:58,ret20:10,ret5:2,breakout20:1,atr14:2+(i%3)*.1,close:100+i*.25,validationStatus:'VALID',migrationValidationStatus:'VALID'}))}
function canonicalRows(size=80){return Array.from({length:size},(_,i)=>({snapshotId:`SN-${String(i).padStart(2,'0')}`,securityId:`SEC-${String(i).padStart(2,'0')}`,ticker:`T${String(i).padStart(2,'0')}`,sessionDate:'2026-09-15',validationStatus:'VALID',migrationValidationStatus:'VALID',ohlc:{open:99+i*.25,high:102+i*.25,low:98+i*.25,close:100+i*.25},volume:1000000+i*1000,turnover:30000000+i*10000,technicalInputs:{return1Pct:1+(i%5)*.05,return5Pct:2.5,return20Pct:6,aboveSma20:true,aboveSma50:true,volatility20AnnualizedPct:24,relativeVolume20:1.4},supportResistanceInputs:{asOfSessionDate:'2026-09-15'}}))}
function guardHistory(n=60){return Array.from({length:n},(_,i)=>{const d=new Date(Date.UTC(2026,6,1+i)).toISOString().slice(0,10);return{sessionDate:d,close:100+i*.1,volume:1000000+i,turnover:30000000+i*1000,validationStatus:'VALID',migrationValidationStatus:'VALID'}})}
function fixture(extra={}){return Object.assign({sessionDate:'2026-09-15',canonicalSnapshot:{snapshotId:'UNIVERSE-2026-09-15',sessionDate:'2026-09-15',rows:canonicalRows()},historyIdentities:{range:'validation-approved-through-2026-09-15'},historyByTicker:{},modelGuardHistory:guardHistory(),modelTrainingSessions:trainSessions(),modelCurrentRows:currentRows(),capitalEgp:1000000,approvedStrategyVersions:{PORTFOLIO_BASKET_EQUAL_WEIGHT:SPECS.PORTFOLIO_BASKET_EQUAL_WEIGHT.sourceCommit},approvedConfig:{basketSize:3},requestedStrategyIds:Object.keys(SPECS),generatedAt:'2026-09-15T20:00:00+03:00',applicationVersion:'ASTRA_SHADOW_G09',codeVersion:P.VERSION.pipeline},extra)}
const primary=P.runUnifiedDecisionPipeline(fixture()),zero=P.runUnifiedDecisionPipeline(fixture({modelCurrentRows:currentRows(80,true)})),repeat=P.runUnifiedDecisionPipeline(fixture({generatedAt:'2026-09-15T20:30:00+03:00'}));
const future=fixture();future.canonicalSnapshot.rows[0].supportResistanceInputs.asOfSessionDate='2026-09-16';const temporal=P.runUnifiedDecisionPipeline(future);
const bad=fixture();bad.canonicalSnapshot.rows[0].migrationValidationStatus='UNRESOLVED';const quarantine=P.runUnifiedDecisionPipeline(bad);
const qedge=P.runUnifiedDecisionPipeline(fixture({quantEdge:{score:999,signal:'BUY'}}));
const productionEligible=Object.keys(SPECS).filter(id=>P.productionEligibility(id,fixture(),P.computeRegime(fixture())).productionEligible),retiredExperimental=Object.values(SPECS).filter(x=>['retired','experimental'].includes(x.lifecycleStatus));
const failureIsolation=P.runUnifiedDecisionPipeline(fixture({requestedStrategyIds:['UNKNOWN_RESEARCH','PORTFOLIO_BASKET_EQUAL_WEIGHT']})).ok===true,staleSessionData=P.validateContext(Object.assign(fixture(),{sessionDate:'2026-09-16'})).ok===false;
const out={schemaVersion:'astra-g09-certification-observation-1',version:P.VERSION,config:P.CONFIG,diagnosticCodes:P.DIAGNOSTIC_CODES,specs:SPECS,primary,zero,repeat,temporal,quarantine,qedge,productionEligible,retiredExperimental,failureIsolation,staleSessionData};
const target=process.argv[2]||'/tmp/g09-cert-observation.json';fs.writeFileSync(target,JSON.stringify(out,null,2)+'\n');console.log('G09_CERT_OBSERVATION '+JSON.stringify({target,productionEligible,retiredExperimental:retiredExperimental.length,primary:primary.status,zero:zero.status}));
