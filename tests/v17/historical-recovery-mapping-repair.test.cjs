'use strict';const test=require('node:test'),assert=require('node:assert/strict'),fs=require('fs'),path=require('path');const {validateEvidence}=require('../../scripts/v17/historical-recovery/long-history/validate-mapping-repairs.cjs');
test('mapping repair requires identity, exchange, history and matching source symbol',()=>{const mapping={yahooSymbol:'TEST.CA',identityEvidence:{exactReturnedSymbol:true,exchange:'EGX',tenYearDailySessions:2400,yahooSearchCompanyMatch:'Test',repositoryPriceDifferencePct:1}},doc={sourceSymbol:'TEST.CA',sessionCount:2400};assert.equal(validateEvidence('TEST',mapping,doc).status,'REPAIRED')});
test('ticker similarity without company identity is rejected',()=>{const mapping={yahooSymbol:'TEST.CA',identityEvidence:{exactReturnedSymbol:true,exchange:'EGX',tenYearDailySessions:2400}},doc={sourceSymbol:'TEST.CA',sessionCount:2400};assert.equal(validateEvidence('TEST',mapping,doc).status,'UNRESOLVED')});
test('repair overrides are scoped to V17 and do not replace shared map',()=>{const root=path.resolve(__dirname,'../..'),overrides=JSON.parse(fs.readFileSync(path.join(root,'data/v17/historical-recovery/long-history/yahoo-mapping-overrides.json'),'utf8')),shared=JSON.parse(fs.readFileSync(path.join(root,'data/symbol-map.json'),'utf8'));assert.equal(overrides.scope,'V17_HISTORICAL_RECOVERY_ONLY');assert.equal(overrides.sharedSymbolMapModified,false);assert.equal(shared.EGBE.currency,'EGP');assert.equal(overrides.mappings.EGBE.currency,'USD')});
