import test from 'node:test';
import assert from 'node:assert/strict';
import { buildResearchPublication,verifyResearchPublication } from '../src/research-publication.js';
function strategy(){return{authorityMode:'RESEARCH',researchOnly:true,productionAuthority:false,automaticOrders:false,signalSession:'2026-08-31',strategySnapshotHash:'a'.repeat(64),validation:{accepted:true,selectedPreset:'CONTROLLED_PULLBACK',method:'TRAIN_THEN_OOS',selectedOutOfSample:{triggered:343,target1OrBetter:175,target1HitRatePct:51.02,stops:111,stopRatePct:32.36,expectancyR:.233,profitFactor:1.612}},authority:{researchRecommendations:true},recommendations:[{ticker:'TEST',companyNameAr:'شركة اختبار',decision:'BUY_CANDIDATE',executableResearchPlan:true,authorityMode:'RESEARCH',researchOnly:true,productionAuthority:false,automaticOrders:false,qualityScore:88,entryLow:10,entryHigh:10.1,stop:9.6,target1:10.8,target2:11.4,netRiskReward:1.3,entryExpirySessions:4,horizonSessions:12,planHash:'b'.repeat(64),diagnostics:{close:10.05,rsi14:60,atrPct:2,momentum20Pct:8,relativeVolume20:1.4,medianTradedValue20:20_000_000,distanceToPrior20dHighPct:-2}}]}}
test('accepted research strategy becomes hash-bound publication without production authority',()=>{const p=buildResearchPublication(strategy());assert.equal(p.counts.recommendations,1);assert.equal(p.productionAuthority,false);assert.equal(p.recommendations[0].target1,10.8);assert.match(p.publicationHash,/^[a-f0-9]{64}$/);assert.equal(verifyResearchPublication(p),true)});
test('publication rejects unaccepted strategy',()=>{const s=strategy();s.validation.accepted=false;assert.throws(()=>buildResearchPublication(s),/NOT_ACCEPTED/)});
test('publication rejects malformed executable geometry',()=>{const s=strategy();s.recommendations[0].stop=10.2;assert.throws(()=>buildResearchPublication(s),/INVALID_PLAN/)});
test('publication verifier detects tampering',()=>{const p=buildResearchPublication(strategy());const x={...p,recommendations:p.recommendations.map(r=>({...r,target1:r.target1+1}))};assert.equal(verifyResearchPublication(x),false)});
