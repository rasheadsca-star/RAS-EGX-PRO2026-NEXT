import test from 'node:test';import assert from'node:assert/strict';import{buildUniverseRegistry}from'../src/universe.js';import{createSessionManifest,validateSessionManifest}from'../src/session-manifest.js';
const bars=Array.from({length:100},(_,i)=>({session:i===99?'2026-08-31':`S${i}`,open:1,high:1,low:1,close:1,volume:100}));const symbols=[{ticker:'AAA',bars},{ticker:'BBB',bars}];
test('registry is deterministic',()=>assert.deepEqual(buildUniverseRegistry(symbols,{latestSession:'2026-08-31'}),buildUniverseRegistry(symbols,{latestSession:'2026-08-31'})));
test('manifest hash validates and tampering fails',()=>{const m=createSessionManifest({marketSession:'2026-08-31',exchangeCalendarVersion:'v1',universeVersion:'u1',rawDataVersion:'r1',normalizedDataVersion:'n1',sourceManifest:{a:'x'},corporateActionVersion:'c1',fundamentalsCutoff:'2026-08-31T00:00:00Z',featureVersion:'f0',modelVersion:'baseline0',engineVersion:'0.1.0',configVersion:'cfg0',commitHash:'TEST',generatedAt:'2026-08-31T14:00:00Z'});assert.equal(validateSessionManifest(m).valid,true);const t={...m,marketSession:'2026-08-30'};assert.equal(validateSessionManifest(t).valid,false)});
