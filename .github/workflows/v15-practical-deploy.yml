name: EGX Professional V16.3 Validate Test and Deploy — RETIRED BY G20

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/v15-practical-deploy.yml"

permissions:
  contents: read

concurrency:
  group: egx-professional-v16-3-pages
  cancel-in-progress: true

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired by G20. Canonical production publisher is .github/workflows/static.yml."
