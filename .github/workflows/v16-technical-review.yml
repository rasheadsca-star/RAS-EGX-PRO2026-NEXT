name: EGX V16.3 Technical Review 20 Cycles — RETIRED BY G20

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/v16-technical-review.yml"

permissions:
  contents: read

concurrency:
  group: egx-v16-3-technical-review
  cancel-in-progress: true

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired by G20. Canonical production publisher is .github/workflows/static.yml."
