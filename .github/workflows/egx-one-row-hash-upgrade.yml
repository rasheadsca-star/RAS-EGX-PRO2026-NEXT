name: EGX ONE SSOT Row Hash Upgrade (completed)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  completed:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Raw and normalized row-hash admission guards already applied; workflow intentionally disabled.'
