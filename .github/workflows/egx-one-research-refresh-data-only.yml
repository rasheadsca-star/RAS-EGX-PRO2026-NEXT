name: EGX ONE Research Market Refresh Data Only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-definition:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Canonical scheduled definition is .github/workflows/egx-one-research-refresh.yml; this marker prevents reintroducing Vercel deploy into research refresh.'
