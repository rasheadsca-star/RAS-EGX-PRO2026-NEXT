name: EGX V16.7 Coherent Engine

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "scripts/research/v167_core.py"
      - "scripts/research/v16-v167-coherent-engine.py"
      - "scripts/research/v16-v167-run.py"
      - ".github/workflows/v16-v167-coherent-engine.yml"
  push:
    paths:
      - "scripts/research/v167_core.py"
      - "scripts/research/v16-v167-coherent-engine.py"
      - "scripts/research/v16-v167-run.py"
      - ".github/workflows/v16-v167-coherent-engine.yml"
  schedule:
    - cron: "55 12 * * 0-4"

permissions:
  contents: write

concurrency:
  group: egx-v167-coherent-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate source
        run: |
          python -m py_compile scripts/research/v167_core.py
          python -m py_compile scripts/research/v16-v167-coherent-engine.py
          python -m py_compile scripts/research/v16-v167-run.py

      - name: Run coherent nested walk-forward
        run: python scripts/research/v16-v167-run.py

      - name: Upload V16.7 report
        uses: actions/upload-artifact@v4
        with:
          name: v16-v167-coherent-engine-report
          path: data/research/v16-v167-coherent-engine.json

      - name: Commit report on main only
        if: github.event_name != 'pull_request'
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "4189822+github-actions[bot]@users.noreply.github.com"
          git add data/research/v16-v167-coherent-engine.json
          if ! git diff --cached --quiet; then
            git commit -m "Publish V16.7 coherent engine report"
            for attempt in 1 2 3; do
              if git pull --rebase -X ours origin main && git push origin HEAD:main; then break; fi
              [ "$attempt" = "3" ] && exit 1
              sleep $((attempt * 8))
            done
          fi
