name: MAIN APP Protected V16.9 Deploy — RETIRED BY G20

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/v169-primary-pages-deploy.yml"

permissions:
  contents: read

concurrency:
  group: main-app-v169-protected-pages
  cancel-in-progress: true

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired by G20. Canonical production publisher is .github/workflows/static.yml."
