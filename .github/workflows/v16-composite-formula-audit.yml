name: EGX V16 Composite Formula Audit

on:
  workflow_dispatch:
  push:
    paths:
      - "scripts/research/v16-composite-formula-audit.cjs"
      - ".github/workflows/v16-composite-formula-audit.yml"

permissions:
  contents: write

concurrency:
  group: egx-v16-composite-formula-audit
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Validate formula audit
        run: node --check scripts/research/v16-composite-formula-audit.cjs

      - name: Apply formula to prior session
        run: node scripts/research/v16-composite-formula-audit.cjs

      - name: Commit audit result
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/research/v16-composite-formula-audit.json
          if ! git diff --cached --quiet; then
            git commit -m "Evaluate composite formula against top gainers"
            for attempt in 1 2 3; do
              if git pull --rebase -X ours origin main && git push origin HEAD:main; then break; fi
              [ "$attempt" = "3" ] && exit 1
              sleep $((attempt * 8))
            done
          fi
