name: EGX ONE SSOT Finalization Hash Upgrade (completed)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  completed:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'SSOT first-finalization hash guards already applied; workflow intentionally disabled.'
